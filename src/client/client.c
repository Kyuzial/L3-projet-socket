#include <stdio.h> 
#include <sys/socket.h> 
#include <arpa/inet.h> 
#include <unistd.h> 
#include <string.h> 

#define PORT 8080

int main()
{
    int connect(int sockfd, const struct sockaddr *addr, socklen_t addrlen);
    return 1;
}